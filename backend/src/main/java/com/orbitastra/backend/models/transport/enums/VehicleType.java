package com.orbitastra.backend.models.transport.enums;

/** What kind of vehicle the school runs. */
public enum VehicleType {
    /** A full-size school bus. */
    BUS,

    /** A smaller bus, usually for shorter or thinner routes. */
    MINI_BUS,

    /** A van. */
    VAN,

    /** A tempo traveller. */
    TEMPO_TRAVELLER,

    /** A car, usually for staff or for one or two students. */
    CAR
}
